# 🚀 상품 매칭 프로그램 - 빠른 시작 가이드

## 📌 개요

이 프로그램은 Google Sheets의 주문 데이터와 Excel 상품 마스터 파일을 연동하여 자동으로 상품을 매칭해주는 웹 애플리케이션입니다.

## 🎯 주요 기능

1. **자동 매칭**: 상품명 100% 일치 또는 모델명 매칭
2. **수동 검색**: 유사도 기반 상품 추천 (Fuzzy Search)
3. **실시간 연동**: Google Sheets 데이터 실시간 조회
4. **이미지 미리보기**: 상품 이미지 URL 자동 표시

## 📝 사전 준비사항

### 1. Node.js 설치
- [Node.js 공식 사이트](https://nodejs.org)에서 LTS 버전 다운로드 및 설치
- 설치 확인:
  ```bash
  node --version
  npm --version
  ```

### 2. Google Sheets API 설정

#### 2-1. Google Cloud 프로젝트 생성
1. [Google Cloud Console](https://console.cloud.google.com) 접속
2. 새 프로젝트 생성 (예: "상품매칭프로그램")
3. 프로젝트 선택

#### 2-2. Google Sheets API 활성화
1. 좌측 메뉴 > "API 및 서비스" > "라이브러리"
2. "Google Sheets API" 검색
3. 활성화 클릭

#### 2-3. 서비스 계정 생성
1. "API 및 서비스" > "사용자 인증 정보"
2. "+ 사용자 인증 정보 만들기" > "서비스 계정"
3. 서비스 계정 이름 입력 (예: "product-matcher")
4. "만들고 계속하기" 클릭
5. 역할 선택: "편집자" 선택 후 완료

#### 2-4. JSON 키 파일 다운로드
1. 생성된 서비스 계정 클릭
2. "키" 탭 > "키 추가" > "새 키 만들기"
3. JSON 형식 선택
4. 다운로드된 JSON 파일을 아래 경로에 저장:
   ```
   C:\Users\pc\Desktop\product-matching-react\backend\config\Google Sheets API.json
   ```

#### 2-5. 스프레드시트 공유
1. Google Sheets에서 "상품매칭용시트" 열기
2. 공유 버튼 클릭
3. 서비스 계정 이메일 주소 입력 (JSON 파일의 `client_email` 값)
4. 편집자 권한으로 공유

## 🔧 설치 및 실행

### 1단계: 프로젝트 폴더로 이동
```bash
cd C:\Users\pc\Desktop\product-matching-react
```

### 2단계: Frontend 의존성 설치
```bash
npm install
```

### 3단계: Backend 의존성 설치
```bash
cd backend
npm install
cd ..
```

### 4단계: Backend 서버 시작
**새 터미널 창 1 열기**
```bash
cd C:\Users\pc\Desktop\product-matching-react\backend
npm start
```

✅ 출력 확인:
```
🚀 Server running on port 5000
✅ Google Sheets client initialized
```

### 5단계: Frontend 서버 시작
**새 터미널 창 2 열기**
```bash
cd C:\Users\pc\Desktop\product-matching-react
npm start
```

✅ 자동으로 브라우저가 열리고 `http://localhost:3000` 접속

## 💻 사용 방법

### 1. 엑셀 파일 업로드
1. 왼쪽 사이드바 "📁 엑셀 파일 업로드" 클릭
2. 상품 마스터 Excel 파일 선택 (.xlsx, .xls)
3. "✅ N개 탭 로드 완료" 메시지 확인

### 2. 상품 검색

#### 방법 1: 자동 매칭 (추천)
1. "주문 상품명" 입력란에 검색할 상품명 입력
2. "🤖 자동 매칭" 버튼 클릭
3. 100% 일치하는 상품이 있으면 자동으로 매칭

#### 방법 2: 수동 검색
1. "주문 상품명" 입력란에 검색할 상품명 입력
2. 유사도 기준 조정 (기본: 70%)
3. 추천 상품 개수 설정 (기본: 5개)
4. "🔍 유사 상품 검색" 버튼 클릭

### 3. 매칭 결과 확인
- 유사도 순으로 정렬된 상품 목록 표시
- 각 상품의 정보 확인:
  - 📂 탭: Excel 파일의 시트 이름
  - 💰 입고: 입고가격
  - 💵 공급가: 공급가격
  - 🏢 업체: 공급 업체명
  - 🖼️ 이미지: 상품 이미지 미리보기

### 4. 매칭 확정
- "✅ 매칭하기" 버튼 클릭
- (추후 Google Sheets에 자동 업데이트 기능 추가 예정)

## 📊 Google Sheets 연동

### 스프레드시트 구조
```
시트1 (주문 데이터)
- 상품명 컬럼 필수
- 매칭상품_상품명, 매칭_매입, 매칭_매출 등 자동 생성

매칭상품 (매칭 결과)
- 자동 생성되는 시트
- 매칭 완료된 상품 목록
```

## 🎨 설정 조정

### 유사도 기준 조정
- 30% ~ 100% 범위 설정 가능
- 높을수록 정확한 매칭, 낮을수록 넓은 범위 검색

### 추천 상품 개수
- 3개 ~ 10개 범위 설정 가능
- 많을수록 더 다양한 옵션 제공

## 🔍 트러블슈팅

### ❌ Backend 연결 실패
**증상**: "Google Sheets client not initialized" 오류

**해결 방법**:
1. `backend/config/Google Sheets API.json` 파일 존재 확인
2. JSON 파일 형식이 올바른지 확인
3. Backend 서버 재시작

### ❌ 스프레드시트를 찾을 수 없음
**증상**: "Spreadsheet not found" 오류

**해결 방법**:
1. Google Sheets에서 서비스 계정 이메일로 공유되었는지 확인
2. 스프레드시트 이름이 "상품매칭용시트"인지 확인 (대소문자 구분)

### ❌ 파일 업로드 실패
**증상**: 파일 업로드 후 오류 발생

**해결 방법**:
1. Excel 파일 형식 확인 (.xlsx, .xls)
2. 파일이 열려있지 않은지 확인 (다른 프로그램에서 사용 중)
3. 파일 크기가 너무 크지 않은지 확인 (권장: 10MB 이하)

### ❌ 포트 충돌
**증상**: "Port 5000 is already in use" 오류

**해결 방법**:
1. Backend server.js에서 포트 변경 (예: 5001)
2. Frontend .env 파일에서 `REACT_APP_API_URL=http://localhost:5001` 변경
3. 서버 재시작

## 📚 추가 정보

### API 엔드포인트
- `GET /api/health` - 서버 상태 확인
- `POST /api/upload-excel` - Excel 파일 업로드
- `POST /api/find-matches` - 유사 상품 검색
- `POST /api/auto-match` - 자동 매칭

### 파일 위치
```
product-matching-react/
├── src/
│   ├── components/
│   │   ├── FileUpload.js       # 파일 업로드 컴포넌트
│   │   ├── ProductMatching.js  # 상품 매칭 메인
│   │   ├── MatchCard.js        # 매칭 결과 카드
│   │   └── SpreadsheetViewer.js # 시트 뷰어
│   ├── App.js                   # 메인 앱
│   └── index.css                # 스타일
├── backend/
│   ├── config/
│   │   └── Google Sheets API.json  # Google API 키
│   ├── server.js                # Express 서버
│   └── matcher.js               # 매칭 로직
└── README.md
```

## 💡 팁

1. **자동 매칭 우선 사용**: 정확도가 높고 빠름
2. **유사도 70-80% 권장**: 너무 낮으면 관련 없는 상품 표시
3. **이미지 확인**: 매칭 전 이미지로 상품 확인
4. **탭 이름 확인**: 엑셀 파일의 탭 이름이 정확한지 확인

## 🤝 지원

문제가 발생하면:
1. 터미널의 오류 메시지 확인
2. Browser 개발자 도구 (F12) 콘솔 확인
3. README.md 파일의 트러블슈팅 섹션 참고

---

**버전**: 2.0 (React)
**마지막 업데이트**: 2025-12-08
