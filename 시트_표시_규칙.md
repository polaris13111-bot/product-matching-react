# 시트 표시 규칙

자동매칭이 구글 시트에 값을 채울 때 칠하는 색과, 금액을 계산하는 방식입니다.

> 색깔 값의 원본은 `backend/server.js` 의 `COLOR` 입니다.
> 앱 화면 왼쪽 아래 "표시 규칙"(`src/components/HelpLegend.js`)이 이 문서의 요약본입니다.
> 셋 중 하나를 고치면 나머지도 같이 고쳐야 합니다.

---

## 색깔이 뜻하는 것

| 색 | 어디에 | 무슨 뜻 |
|---|---|---|
| 🟧 주황 | 행 전체 | 매입 업체 이름에 **내셔널 / 내셔날**이 들어감 |
| 🟩 형광 초록 | 수량 칸, 매칭_매입 칸 | 수량이 **2개 이상** (매입 금액에 수량이 곱해짐) |
| 🟥 빨강 | 매칭_매입 · 매칭_매출 | 역마진 (파는 값이 사는 값보다 쌈) |
| 🟥 빨강 | 매칭_탭 | 단종 상품 |
| 🟨 노랑 | 매칭_매출 | 동공급가가 아직 안 정해짐 (값은 비워둠) |
| 🟢 연한 초록 | 채워진 칸 | 이번에 새로 채운 칸 |

한 칸에 여러 색이 겹치면 **빨강 > 노랑 > 형광 초록 > 주황 > 연한 초록** 순으로 이깁니다.

주황 행에서는 이번에 새로 채운 칸도 **연한 초록이 아니라 주황**으로 칠합니다.
안 그러면 채운 칸만 초록이 되어 행 경고가 중간에 끊겨 보입니다.
단 빨강·노랑·형광 초록은 주황을 이기므로 진짜 경고는 그대로 보입니다.

---

## 금액 계산

- **매칭_매입** = (매입가 + 배송비) × 수량
  - 수량이 **2 이상일 때만** 곱합니다. 1개거나 수량 칸이 비어 있으면 그냥 (매입가 + 배송비).
  - ⚠️ **배송비도 같이 곱해집니다.** 3개면 배송비가 3번 붙습니다.
- **매칭_매출** = 동공급가 + 배송비
  - ⚠️ **수량을 곱하지 않습니다.** 그래서 수량이 많은 행은 매입만 커 보여 손해처럼 착시가 생길 수 있습니다.

---

## 안 건드리는 것

- **이미 값이 들어 있는 칸은 값도 색도 손대지 않습니다.** 빈 칸만 채웁니다.
- **수량 칸은 색만 칠하고 숫자는 절대 안 바꿉니다.**
- 시트에 없는 컬럼은 새로 만들지 않고 그냥 넘어갑니다.
  이때 **화면 위에 빨간 경고 띠가 뜹니다.** 특히 `수량` 머리글이 없거나 이름이 바뀌면
  매칭_매입에 수량이 안 곱해진 1개 단가가 들어가므로, 그 사실을 그대로 알려줍니다.

---

## 컬럼 찾는 방식

**열 위치(N열 등)가 아니라 첫 줄에 적힌 글자로 찾습니다.** 열을 중간에 하나 끼워 넣어도 안 깨집니다.
대신 **헤더 글자가 바뀌면 그 컬럼은 못 찾습니다.** 수량 칸의 머리글은 정확히 `수량` 이어야 합니다.

---

## 알아둘 함정

업체 조건이 "이름에 내셔널/내셔날이 들어가면"이라서, **`신영인터내셔널` · `HS인터내셔널` 같은 무관한 회사도 걸립니다.**
2026-08-01 기준 이 회사들은 등록된 상품이 0개라 실제로는 아무 일도 안 일어납니다.
나중에 이 회사 상품이 등록되면 엉뚱한 행이 주황으로 칠해지므로, 그때 `backend/server.js` 의 `WARN_SUPPLIER_RE` 를 좁혀야 합니다.
